@model IEnumerable<Student>
@{
    Layout = "_Layout";
}
<h3>Список всех учеников с оценками</h3>
<table class="table table-bordered table-striped" style="width:50%">
    <thead>
        <tr>
            <th>
                Студент
            </th>
            <th>
                Оценки
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var s in Model)
        {
            <tr>
                <td width="25%">@s.Name @s.LastName @s.SecondName</td>
                <td width="25%" align="center">
                    @foreach (var m in s.Scores)
                    {
                        @m.ScoreValue.ToString();
                    }
                </td>
            </tr>
            
        }
    </tbody>
</table>
<p><a href="/task/1">Перейти к первому заданию</a></p>
<p><a href="/task/2">Перейти ко второму заданию</a></p>
<p><a href="/task/3">Перейти к третьему заданию</a></p>


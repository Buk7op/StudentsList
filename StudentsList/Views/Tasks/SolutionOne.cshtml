@model IEnumerable<Student> 
@{
    Layout = "_LayoutTask";
}
<h3>Список учеников с оценками 4 и 5:</h3>
@foreach (var s in Model.Where(s => s.Scores.All(m => m.ScoreValue >= 4)))
{
        <li>@s.Name @s.LastName @s.SecondName</li>
}